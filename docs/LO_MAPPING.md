# Learning Outcome Mapping

## LO1: Networking concepts (server/client/protocol)
- Flask HTTP request/response routing architecture.
- SSE endpoint (`/events`) for server-push updates.

## LO2: Configure and secure web server for dynamic content
- Dynamic Flask routes with secured session handling.
- Security headers, CSRF middleware, and role protections.

## LO3: Standards-based websites + accessibility
- Jinja2 templates with Bootstrap responsive UI.
- Improved form labels and table caption semantics.

## LO4: Database technologies for the web
- Relational schema using SQLAlchemy models and MySQL.
- Query aggregation and indexed workout table for performance.

## LO5: Full data-driven dynamic website
- Server-side Flask + client-side JS integration.
- User and admin workflows fully DB-backed with real-time refresh.
